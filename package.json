{
  "name": "solarcido",
  "version": "0.1.0",
  "private": true,
  "description": "solarcido CLI built on Upstage solar-pro3-260323",
  "type": "module",
  "bin": {
    "solarcido": "bin/solarcido.js"
  },
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "build:rust": "cargo build -p solarcido-cli",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run build && node tests/cli.test.mjs && node tests/config.test.mjs && node tests/approvals.test.mjs && node tests/sessions.test.mjs && node tests/tools.test.mjs && node tests/execution-guard.test.mjs",
    "typecheck": "npx tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
